version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: jjkt-auth
    restart: unless-stopped
    ports:
      - "9101:80"
    networks:
      - laravel

networks:
  laravel:
    driver: bridge